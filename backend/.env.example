# OpenAI API Key (Optional: System runs locally with zero API key dependencies via deterministic grounded RAG)
OPENAI_API_KEY=your_openai_api_key_here
MODEL_NAME=gpt-4o-mini

# Evaluation & Environment Settings
ENVIRONMENT=production
LOG_LEVEL=INFO
SEED=42
CACHE_ENABLED=true
